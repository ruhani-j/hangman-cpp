# Hangman C++

- Use C++17 and CMake.
- Keep game rules deterministic when practical so they can be tested.
- Validate all interactive input.
- Do not commit build output.
- Prefer focused changes and verify with a clean CMake build.
