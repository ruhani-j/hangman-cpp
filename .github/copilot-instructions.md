# Project Instructions

Use C++17 and the existing CMake target. Keep game rules separate from terminal rendering as the project grows. Validate input, preserve warning-clean builds, and avoid committing `build/` or generated binaries.
